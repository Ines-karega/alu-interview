# Rain

Calculate how many square units of water will be retained after it rains, given a list of non-negative integers representing wall heights.

## Prototype

```python
def rain(walls)
```

## Example

```
walls = [0, 1, 0, 2, 0, 3, 0, 4]
rain(walls)  # returns 6
```
